import { Box } from "@mui/material";
import { styled } from "@mui/system";

const InlineGrid = styled(Box)({
 
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'space-evenly',
  alignItems: 'center',
  height: '50%',
 
});

export default InlineGrid;
