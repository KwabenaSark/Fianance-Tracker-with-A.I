import { Box } from "@mui/material";
import { styled } from "@mui/system";

const PlaceCenter = styled(Box)({
 
  display: 'grid',
  placeItems: "center",
  alignItems: 'center',
  height: '80%'
});

export default PlaceCenter;
